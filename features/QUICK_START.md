# Match3D quick start

Run the default 3D match from the Godot editor or with:

```sh
godot --path .
```

The field uses `Vector3` world coordinates: X is length, Y is height, and Z is
width. Use WASD to move, I to sprint, K to pass, O to lob, J to shoot, and U to
tackle.
